name: 'Setup'
description: 'Installs dependencies and builds the project with Meson on Linux and Windows (MSYS2)'
inputs:
  buildtype:
    description: 'Meson build type (release or debug)'
    required: true
    default: 'release'

runs:
  using: "composite"
  steps:
    - name: Add LunarG Vulkan SDK repository
      if: runner.os == 'Linux'
      shell: bash
      run: |
        wget -qO- https://packages.lunarg.com/lunarg-signing-key-pub.asc | sudo tee /etc/apt/trusted.gpg.d/lunarg.asc
        sudo wget -qO /etc/apt/sources.list.d/lunarg-vulkan-noble.list https://packages.lunarg.com/vulkan/lunarg-vulkan-noble.list
        sudo apt-get update

    - name: Install Linux dependencies
      if: runner.os == 'Linux'
      shell: bash
      run: |
        sudo apt-get install -y \
          gcc \
          meson \
          ninja-build \
          libvulkan-dev \
          vulkan-headers \
          shaderc \
          vulkan-utility-libraries-dev \
          libassimp-dev \
          libcglm-dev \
          libsixel-dev \
          pkg-config \
          libvips-dev \
          python3-pytest

    - name: Setup MSYS2
      if: runner.os == 'Windows'
      uses: msys2/setup-msys2@v2
      with:
        msystem: clang64
        update: true
        install: >-
          mingw-w64-clang-x86_64-toolchain
          mingw-w64-clang-x86_64-meson
          mingw-w64-clang-x86_64-ninja
          mingw-w64-clang-x86_64-cmake
          mingw-w64-clang-x86_64-pkgconf
          mingw-w64-clang-x86_64-vulkan-headers
          mingw-w64-clang-x86_64-vulkan-loader
          mingw-w64-clang-x86_64-shaderc
          mingw-w64-clang-x86_64-assimp
          mingw-w64-clang-x86_64-cglm
          mingw-w64-clang-x86_64-libvips
          mingw-w64-clang-x86_64-python
          mingw-w64-clang-x86_64-python-pytest
          git

    - name: Configure + Build (Linux)
      if: runner.os == 'Linux'
      shell: bash
      run: |
        BUILDTYPE=$(echo "${{ inputs.buildtype }}" | tr '[:upper:]' '[:lower:]')
        meson setup build -Dbuildtype=$BUILDTYPE
        meson compile -C build
        meson test -C build --print-errorlogs

    - name: Configure + Build (Windows)
      if: runner.os == 'Windows'
      shell: msys2 {0}
      run: |
        BUILDTYPE=$(echo "${{ inputs.buildtype }}" | tr '[:upper:]' '[:lower:]')
        meson setup buildDir -Dbuildtype=$BUILDTYPE -Ddefault_library=shared -Dbundled_libsixel=enabled
        meson compile -C buildDir
        meson test -C buildDir --print-errorlogs
