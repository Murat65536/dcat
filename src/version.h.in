#pragma once

// Generated by Meson's vcs_tag() at build time. @VCS_TAG@ expands to the output
// of `git describe`, or to the project() version from meson.build when building
// outside a git checkout (e.g. a release tarball).
#define DCAT_VERSION "@VCS_TAG@"
